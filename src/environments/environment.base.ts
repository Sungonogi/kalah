export const baseEnvironment = {
    engineTimeLimit: 30,
    minimumWaitTime: 0, // if the computer is too fast, wait at least this long
    disableAudio: true,
    gameOverAnimationTime: 800,
    gameOverModalTime: 200,
};
