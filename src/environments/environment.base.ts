export const baseEnvironment = {
    engineTimeLimit: 50,
    minimumWaitTime: 500, // if the computer is too fast, wait at least this long
    disableAudio: false,
    gameOverAnimationTime: 800,
    gameOverModalTime: 200,
};
